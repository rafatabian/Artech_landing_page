export default funtion robots() {
const baseURL = "https://artech-agency.co"
return {
    rules{
        userAgent: "*",
        allow: ["/"],
        disallow: ["/contact", "/privacy", "/terms"]
    },
    sitemap: `${baseURL}/sitemap.xml`
}
}